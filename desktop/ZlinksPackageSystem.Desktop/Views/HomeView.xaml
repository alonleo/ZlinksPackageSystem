<UserControl x:Class="ZlinksPackageSystem.Desktop.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="首页" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Game Card -->
            <Border Grid.Column="0" Background="#FF409EFF" CornerRadius="10" Margin="10" Padding="20">
                <StackPanel>
                    <TextBlock Text="游戏数量" Foreground="White" FontSize="16"/>
                    <TextBlock Text="{Binding GameCount}" Foreground="White" FontSize="48" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Product Card -->
            <Border Grid.Column="1" Background="#FF67C23A" CornerRadius="10" Margin="10" Padding="20">
                <StackPanel>
                    <TextBlock Text="产品数量" Foreground="White" FontSize="16"/>
                    <TextBlock Text="{Binding ProductCount}" Foreground="White" FontSize="48" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Test Card -->
            <Border Grid.Column="2" Background="#FFE6A23C" CornerRadius="10" Margin="10" Padding="20">
                <StackPanel>
                    <TextBlock Text="测试数量" Foreground="White" FontSize="16"/>
                    <TextBlock Text="{Binding TestCount}" Foreground="White" FontSize="48" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>