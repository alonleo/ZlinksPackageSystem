<UserControl x:Class="ZlinksPackageSystem.Desktop.Views.GameListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="游戏管理" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="新增游戏" 
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    HorizontalAlignment="Right"
                    Width="120"/>
        </DockPanel>

        <!-- Search Bar -->
        <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="15" Margin="0,0,0,20">
            <DockPanel>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="游戏名称"
                         Width="200"
                         Margin="0,0,10,0"/>
                <Button Content="搜索" 
                        Command="{Binding SearchCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Width="80"/>
            </DockPanel>
        </Border>

        <!-- Data Grid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Games}"
                  SelectedItem="{Binding SelectedGame}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="游戏名称" Binding="{Binding GameName}" Width="150"/>
                <DataGridTextColumn Header="游戏方向" Binding="{Binding GameDirection}" Width="100"/>
                <DataGridTextColumn Header="来源" Binding="{Binding Source}" Width="100"/>
                <DataGridTextColumn Header="负责人" Binding="{Binding Manager}" Width="80"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="优先级" Binding="{Binding Priority}" Width="60"/>
                <DataGridTextColumn Header="创建时间" Binding="{Binding CreateTime, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="150"/>
                <DataGridTemplateColumn Header="操作" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="编辑" 
                                        Style="{StaticResource MaterialDesignFlatButton}"
                                        Margin="0,0,5,0"/>
                                <Button Content="删除" 
                                        Command="{Binding DataContext.DeleteGameCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource MaterialDesignFlatButton}"
                                        Foreground="#FFF56C6C"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Pagination -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="上一页" 
                    Command="{Binding PreviousPageCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Width="80"
                    Margin="0,0,10,0"/>
            <TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBlock Text="/" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBlock Text="{Binding TotalCount}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button Content="下一页" 
                    Command="{Binding NextPageCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Width="80"/>
        </StackPanel>
    </Grid>
</UserControl>