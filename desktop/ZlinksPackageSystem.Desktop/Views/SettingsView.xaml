<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" MaxWidth="600">

            <!-- Theme Settings -->
            <materialDesign:Card Margin="0,0,0,16" Padding="20" UniformCornerRadius="10">
                <StackPanel>
                    <TextBlock Text="主题设置" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>

                    <StackPanel Margin="0,0,0,8">
                        <RadioButton GroupName="Theme" Content="浅色模式"
                                     IsChecked="{Binding IsDarkTheme, Converter={StaticResource BooleanInverseConverter}}"
                                     Margin="0,0,0,8"/>
                        <RadioButton GroupName="Theme" Content="深色模式"
                                     IsChecked="{Binding IsDarkTheme}"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>

            <!-- Background Settings -->
            <materialDesign:Card Margin="0,0,0,16" Padding="20" UniformCornerRadius="10">
                <StackPanel>
                    <TextBlock Text="背景设置" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>

                    <RadioButton Content="无背景" GroupName="Bg"
                                 IsChecked="{Binding BackgroundType, Converter={StaticResource RadioBoolConverter}, ConverterParameter=0}"
                                 Margin="0,0,0,8"/>
                    <RadioButton Content="渐变 1" GroupName="Bg"
                                 IsChecked="{Binding BackgroundType, Converter={StaticResource RadioBoolConverter}, ConverterParameter=1}"
                                 Margin="0,0,0,8"/>
                    <RadioButton Content="渐变 2" GroupName="Bg"
                                 IsChecked="{Binding BackgroundType, Converter={StaticResource RadioBoolConverter}, ConverterParameter=2}"
                                 Margin="0,0,0,8"/>

                    <Grid Margin="0,8,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="自定义图片" VerticalAlignment="Center" FontSize="14"/>
                        <Button Grid.Column="1" Content="选择图片"
                                Command="{Binding SelectImageFileCommand}"
                                Style="{StaticResource MaterialDesignFlatButton}"/>
                    </Grid>

                    <TextBlock Text="背景透明度" FontSize="14" Margin="0,0,0,8"/>
                    <Slider Value="{Binding BackgroundOpacity, UpdateSourceTrigger=PropertyChanged}"
                            Minimum="0.1" Maximum="1.0" TickFrequency="0.1" IsSnapToTickEnabled="True"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Update Settings -->
            <materialDesign:Card Padding="20" UniformCornerRadius="10">
                <StackPanel>
                    <TextBlock Text="更新设置" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="自动检测版本更新" VerticalAlignment="Center" FontSize="14"/>
                        <ToggleButton Grid.Column="1"
                                      IsChecked="{Binding AutoCheckUpdates}"
                                      Style="{StaticResource MaterialDesignSwitchToggleButton}" />
                    </Grid>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
