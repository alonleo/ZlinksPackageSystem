<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="LoginView_Loaded">

    <Grid>
        <Rectangle Stretch="UniformToFill" Opacity="0.3">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF0f0c29" Offset="0.0"/>
                    <GradientStop Color="#FF302b63" Offset="0.5"/>
                    <GradientStop Color="#FF24243e" Offset="1.0"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <Border Background="#CC1e1e2e" CornerRadius="12" Padding="36"
                MaxWidth="380"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <StackPanel HorizontalAlignment="Stretch">
                <TextBlock Text="Zlinks Package System" 
                          FontSize="24" 
                          Foreground="White" 
                          HorizontalAlignment="Center" 
                          Margin="0,0,0,24"/>
                
                <TextBlock Text="游戏包管理系统" 
                          FontSize="14" 
                          Foreground="#FFBFcbd9" 
                          HorizontalAlignment="Center" 
                          Margin="0,0,0,32"/>

                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="用户名"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,0,16"/>

                <PasswordBox x:Name="PasswordBox"
                            materialDesign:HintAssist.Hint="密码"
                            Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                            PasswordChanged="PasswordBox_PasswordChanged"
                            Margin="0,0,0,12"/>

                <TextBlock Text="{Binding ErrorMessage}"
                          Foreground="#FFF56C6C"
                          Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"
                          Margin="0,0,0,10"/>

                <CheckBox Content="记住密码"
                          IsChecked="{Binding RememberPassword}"
                          Foreground="#FFBFcbd9"
                          Margin="0,0,0,16"/>

                <Button Content="登录"
                        Command="{Binding DataContext.LoginCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        HorizontalAlignment="Stretch"
                        Height="40"
                        Margin="0,0,0,12"/>

                <Button Content="忘记密码？"
                        Command="{Binding DataContext.ForgotPasswordCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Foreground="#FFBFcbd9"
                        FontSize="12"
                        HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
